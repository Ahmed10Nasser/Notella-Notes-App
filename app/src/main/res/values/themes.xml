<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Notella" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>


    <!-- Search theme. -->
    <style name="ThemeOverlay.search" parent="Theme.Notella">
        <!-- Customize your theme here. -->
        <item name="android:editTextColor">@color/ColorWhite</item>
        <item name="android:textColorHint">@color/ColorText</item>
        <item name="elevationOverlayColor">@color/colorOnSurface</item>
    </style>
</resources>